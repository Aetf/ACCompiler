int i, j, k;
int func2 (int* p);
char func3 ();
int func1 (float f, char ch)
{
	i = f + ch;
	func2 (&i);
	return i;
}

int func2 (int *p)
{
	char str[12, 10, 9];
	i = *p + 1;
	str[i, 8, 7] = 13;
	func3();

	return 0;
}

char func3 ()
{
	if (i == 0)
	{
		float *abc = &i;
		int a = 5, b = 3, c = 12;
		(*abc) = (*abc) + 1;
	}

	return 0;
}

int main()
{
	int *p = &i;
	if (i == 0)
	{
		while (*p>=0)
		{
			do
			{
				i = i+ 1;
			}
			while ( i < 10 || (13 > 3 && i < 14) );
		}
	} else if (i > 0)
	{
		i = 1;
	} else
	{
		i = 2;
	}

	for (i = 0; i!= 4; i = i+1)
	{
		int b = i;
		b = b - 1;
	}
	return i;
}
