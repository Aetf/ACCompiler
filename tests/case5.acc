int i, j, k;
int func2 (int* p);
char func3 ();
int func1 (float f, char ch)
{
	i = f + ch;
	func2 (&i);
	return i;
}

int func2 (int *p)
{
	char str[12] = "Hello";
	i = *p + 1;
	str[i] = 13;
	func3();

	return 0;
}

char func3 ()
{
	if (i == 0)
	{
		float *abc = &i;
		int a = 5, b = 3, c = 12;
		(*abc) = (*abc) + 1;
	}

	return 0;
}

int main()
{
	if (i == 0)
	{
		while ((i+1)>=0)
		{
			do
			{
				i = i+ 1;
			}
			while ( (i-1) < 10 || (13 > 3 && i < 14) );
		}
	} else if (i > 0)
	{
		i = 1;
	} else
	{
		i = 2;
	}
	return i;
}
